// Code generated by cmd/codegen; DO NOT EDIT.
package {{ .Package }}

import "github.com/go-theft-craft/server/pkg/gamedata"

type effectRegistry struct {
	byID   map[int]gamedata.Effect
	byName map[string]gamedata.Effect
	all    []gamedata.Effect
}

func newEffectRegistry() gamedata.EffectRegistry {
	all := []gamedata.Effect{
		{{- range .Data }}
		{
			ID: {{ .ID }},
			Name: {{ printf "%q" .Name }},
			DisplayName: {{ printf "%q" .DisplayName }},
			Type: {{ printf "%q" .Type }},
		},
		{{- end }}
	}

	byID := make(map[int]gamedata.Effect, len(all))
	byName := make(map[string]gamedata.Effect, len(all))
	for _, v := range all {
		byID[v.ID] = v
		byName[v.Name] = v
	}

	return &effectRegistry{byID: byID, byName: byName, all: all}
}

func (r *effectRegistry) ByID(id int) (gamedata.Effect, bool)       { v, ok := r.byID[id]; return v, ok }
func (r *effectRegistry) ByName(name string) (gamedata.Effect, bool) { v, ok := r.byName[name]; return v, ok }
func (r *effectRegistry) All() []gamedata.Effect                     { return r.all }
